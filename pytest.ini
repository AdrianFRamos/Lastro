[pytest]
addopts = -ra
pythonpath = hardware-simulator/src
markers =
    hardware: requires the physical ESP32-C5 Station and RFID hardware; never counts as passed in repository-only CI
    system: requires a running Lastro full-stack environment and, where specified, local validator or Devnet
